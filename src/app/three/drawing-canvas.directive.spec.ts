import { DrawingCanvasDirective } from './drawing-canvas.directive';

describe('DrawingCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawingCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
